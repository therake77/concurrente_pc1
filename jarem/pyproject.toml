[project]
name = "jarem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "therake77", email = "jarema9125@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "torch>=2.14.0",
    "torchvision>=0.29.0",
]

[project.scripts]
jarem = "jarem:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
